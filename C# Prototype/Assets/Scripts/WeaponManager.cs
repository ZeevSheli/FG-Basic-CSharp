using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //destroy all current weapons? or spawn only one of them, probably weapon1, make sure no other weapon is present and there is only ONE of weapon1
    }

    // Update is called once per frame
    void SwitchWeapon()
    {
        // if input is pressed, destroy current weapon unless the weapon is what is being pushed


        // if on the ground, using raycast, the player can jump. Tag could be used.
    }
}

